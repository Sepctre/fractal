{
  "name": "@frctl/handlebars-example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "fractal start --sync",
    "build": "fractal build",
    "test": "jest",
    "test:dev": "npm run test -- --watch"
  },
  "dependencies": {
    "@frctl/fractal": "^0.0.1",
    "@frctl/handlebars": "^0.0.1",
    "@frctl/mandelbrot": "^0.0.1"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "node-fetch": "^2.6.1"
  }
}
